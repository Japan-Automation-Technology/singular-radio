import { episode } from "./episode";
import { learnTerm } from "./learnTerm";
import { transcriptSegment } from "./transcriptSegment";

const schemas = [episode, learnTerm, transcriptSegment];

export default schemas;
